import COLORS from '../../types/colors'

export default function InfoBar() {
    return (
        <div className={`fixed h-[30px] bottom-0 flex items-center justify-end w-screen ${COLORS.bg_gray}`}>
        
        </div>
    )
}